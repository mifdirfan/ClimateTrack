import React from 'react';
import { View, Text } from 'react-native';

export default function WritePostPage() {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>Write a new post here! (native-stack)</Text>
        </View>
    );
}
